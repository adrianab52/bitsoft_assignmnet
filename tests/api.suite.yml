actor: ApiTester
modules:
    enabled:
        - REST:
                url: &url 'https://geocode.search.hereapi.com/v1/geocode'
                depends: PhpBrowser
        - \Helper\Api
    config:
        PhpBrowser:
          url: *url
          headers:
            Content-Type: application/json
